---
- name: Base packages install
  yum:
    name: "{{ packages }}"
  vars:
    packages:
    - wget
    - curl
    - unzip
    - zip
    - bind-utils
    - lsof
    - nfs-utils
    - open-vm-tools
    - yum-utils
    - device-mapper-persistent-data
    - lvm2

- name: Remove packages
  yum:
    name: "{{ packages }}"
    state:  absent
  vars:
    packages:
    - gcc
