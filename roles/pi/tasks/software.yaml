---
- name: Base packages install
  apt:
    name: "{{ packages }}"
  vars:
    packages:
    - nfs-common
    - git

# - name: Remove packages
#   yum:
#     name: "{{ packages }}"
#     state:  absent
#   vars:
#     packages:
#     - gcc
